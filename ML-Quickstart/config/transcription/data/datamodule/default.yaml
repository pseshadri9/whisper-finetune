_target_: transcription.data_utils.datamodule.TranscriptionDataModule
batch_size: 36
num_workers: 6

metadata: ${data.preprocess.metadata}

sample_rate: ${data.preprocess.sample_rate}
num_classes: 88
start_note: 21
segment_length: 10.0
frames_per_second: 100
random_seed: ${seed}
